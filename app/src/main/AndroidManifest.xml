<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.erecruitment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/rounded_corner_button"
        android:supportsRtl="true"
        android:theme="@style/Theme.Erecruitment">
        <activity android:name=".RolldiceActivity"></activity>
        <activity android:name=".CalculatorsActivity" />
        <activity android:name=".CalculatorActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".ResetPassword" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>